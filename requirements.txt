# Python For DevOps – install all deps
# Python>=3.10  (enforced in pyproject.toml; use a venv)
# Create venv: python3 -m venv .venv && source .venv/bin/activate
# Install: pip install -r requirements.txt

# Python>=3.10
psutil>=5.9.0
requests>=2.28.0
boto3>=1.26.0
fastapi>=0.100.0
uvicorn>=0.22.0
